[project]
name = "tok-preparer"
version = "2026.09.04"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "tqdm",
    "pandas<3",
    "matplotlib>=3.11.0",
    "upsetplot>=0.9.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
